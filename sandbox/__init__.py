"""The synthetic sandbox: the customer-support target agent and the tools it can call.

Nothing here touches real systems. Orders, permissions, and the canary secret are
all fake, and the whole store resets between rounds so attacks can't leak state.
"""
